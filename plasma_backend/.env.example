PORT=3000
# Docker (docker-compose) : même identifiants que dans docker-compose.yml
DATABASE_URL=postgresql://plasma:plasma@localhost:5432/plasma
# Si le backend tourne aussi dans Docker, utiliser le nom du service :
# DATABASE_URL=postgresql://plasma:plasma@postgres:5432/plasma
# Plasma Testnet (RPC + chain pour indexer et services)
RPC_URL=https://testnet-rpc.plasma.to
# USDT (Mock) sur Plasma Testnet - pour libérer les cotisations vers le bénéficiaire
USDT_ADDRESS=0x502012b361aebce43b26ec812b74d9a51db4d412
CHAIN_ID=9746
# Adresse Tontine déployée sur le testnet
TONTINE_SERVICE_ADDRESS=0x...
FROM_BLOCK=0
OPENWEATHER_API_KEY=
INSURANCE_CRON=0 */6 * * *
